{
  "name": "augmented-architect-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.5.30",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.1",
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.1",
    "wrangler": "^4.77.0"
  }
}
